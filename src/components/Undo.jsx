import React from 'react';

// import "../styles/Undo.css";

/**
 * @param {object} props 
 */
const Undo = (props) => {
    return (
        <div onClick={} className={""}>
            Undo
        </div>
    );
};

export default Undo;